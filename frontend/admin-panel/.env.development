VITE_API_BASE_URL=/api/v1/
VITE_APP_TITLE=Prompt Teacher Admin
# 端口统一配置在项目根目录的 .env 文件中（DJANGO_PORT / VITE_PORT）
# 修改端口后需重启 Django 和 Vite dev server
# WebSocket端口：开发环境使用Vite dev server时不连接，避免冲突
# 生产环境中若使用daphne服务在同一端口，不需要配置此项
# VITE_WS_PORT=
